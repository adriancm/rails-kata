h1 Landing Page
p Your landing page here

- if user_signed_in?
  = link_to "Sign out", destroy_session_path(current_user), method: :delete
  br
- else
  = link_to "Log in", new_session_path(User.new)
  br
  = link_to "Sign up", new_registration_path(User.new)
  br